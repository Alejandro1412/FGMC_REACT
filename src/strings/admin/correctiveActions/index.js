const useAdminCorrectiveActionsTypes = () => {
  const GET_CORRECTIVE_ACTIONS = "GET_CORRECTIVE_ACTIONS";
  const CREATE_CORRECTIVE_ACTIONS = "CREATE_CORRECTIVE_ACTIONS";

  return { GET_CORRECTIVE_ACTIONS, CREATE_CORRECTIVE_ACTIONS };
};

export default useAdminCorrectiveActionsTypes;
