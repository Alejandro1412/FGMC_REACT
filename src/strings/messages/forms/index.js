const useFormsTypes = () => {
  const REQUIRED_FIELD = "Campo requerido";
  const EMAIL_NOT_VALID = "Email no válido";

  return { REQUIRED_FIELD, EMAIL_NOT_VALID };
};

export default useFormsTypes;
